{
  "name": "ai-battle-arena-backend",
  "version": "1.0.0",
  "description": "Express proxy backend for AI Battle Arena — owns all LLM provider calls, sessions, retries, and logging.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "nanoid": "^5.0.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
